type Goal @model @auth(rules: [{allow: public}]) {
  id: ID!
  icon: String!
  title: String!
  description: String
  repeat: String!
  reminderTime: AWSDateTime
  notify: Boolean
  type: String!
  completed: Boolean!
  owner: AWSEmail
}
 